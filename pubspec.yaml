name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  firebase_core: ^2.30.0
  firebase_auth: ^4.18.0
  cloud_firestore: ^4.15.0
  flutter:
    sdk: flutter
  flutter_tts: ^3.6.3  # ✅ Text-to-Speech

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/   # ✅ Asegúrate de tener esta carpeta y tus imágenes dentro
